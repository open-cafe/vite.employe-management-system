import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#8AFF8A',
    },
    secondary: {
      main: '#ffffff',
    },
    info: {
      main: '#596167',
    },
  },
  typography: {
    fontFamily: 'Roboto',
  },
});
export default theme;
